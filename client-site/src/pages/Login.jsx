import { Helmet } from "react-helmet";


const Login = () => {
    return (
        <div>
            <Helmet>
                <title>Dune || Login</title>
            </Helmet>
            this is login
        </div>
    );
};

export default Login;