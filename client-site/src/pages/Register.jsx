import { Helmet } from "react-helmet";


const Register = () => {
    return (
        <div>
             <Helmet>
                <title>Dune || Register</title>
            </Helmet>
            this is register
        </div>
    );
};

export default Register;